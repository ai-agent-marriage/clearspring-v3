/* custom-tab-bar/index.wxss */
/* Stitch V3.0 禅意定制版 - 确保可见性 */

.tabbar-container {
  position: fixed;
  bottom: 0;
  left: 0;
  right: 0;
  height: 112rpx;
  padding-bottom: env(safe-area-inset-bottom);
  background-color: #EFEEE9; /* 奶米白 */
  display: flex;
  align-items: center;
  justify-content: space-around;
  border-top: 2rpx solid rgba(195, 200, 193, 0.15);
  z-index: 1000; /* 确保高于内容区 */
}

.tabbar-item {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  gap: 12rpx;
  padding: 20rpx 32rpx;
  transition: all 0.3s ease;
}

.tabbar-icon {
  width: 44rpx;
  height: 44rpx;
  color: #718096; /* 未选中灰色 */
  transition: color 0.3s ease;
}

/* 选中态：墨绿 #4A5D4E */
.tabbar-item.active .tabbar-icon {
  color: #4A5D4E;
}

.tabbar-text {
  font-size: 22rpx;
  color: #718096;
  font-family: 'Plus Jakarta Sans', sans-serif;
  letter-spacing: 0.15em;
  transition: color 0.3s ease;
}

/* 选中态：墨绿 #4A5D4E */
.tabbar-item.active .tabbar-text {
  color: #4A5D4E;
  font-weight: 500;
}

.tabbar-item:active {
  opacity: 0.7;
  transform: scale(0.98);
}
