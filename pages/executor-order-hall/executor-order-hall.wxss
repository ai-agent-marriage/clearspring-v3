@import "../../theme.wxss";

/* 清如 ClearSpring - 抢单大厅样式 */

.container {
  padding: var(--stitch-spacing-md);
  padding-bottom: var(--stitch-spacing-xxxl);
  background: linear-gradient(180deg, var(--stitch-bg-primary) 0%, var(--stitch-bg-secondary) 100%);
  min-height: 100vh;
}

/* ========== 筛选栏 ========== */
.filter-bar {
  display: flex;
  justify-content: space-between;
  padding: var(--stitch-spacing-md);
  margin-bottom: var(--stitch-spacing-md);
  background: rgba(255, 255, 255, 0.7);
  backdrop-filter: blur(20rpx);
  -webkit-backdrop-filter: blur(20rpx);
  border-radius: var(--stitch-radius-lg);
}

.filter-item {
  display: flex;
  align-items: center;
  padding: var(--stitch-spacing-sm) var(--stitch-spacing-md);
  transition: all var(--stitch-transition-base);
}

.filter-item:active {
  opacity: 0.7;
}

.filter-text {
  font-size: 28rpx;
  color: var(--stitch-text-primary);
  margin-right: 8rpx;
}

.filter-arrow {
  font-size: 20rpx;
  color: var(--stitch-text-secondary);
}

/* ========== 订单列表 ========== */
.order-list {
  min-height: 500rpx;
}

.empty-state {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  padding: var(--stitch-spacing-xxxl);
  background: rgba(255, 255, 255, 0.5);
  border-radius: var(--stitch-radius-lg);
}

.empty-icon {
  font-size: 100rpx;
  margin-bottom: var(--stitch-spacing-md);
}

.empty-text {
  font-size: 30rpx;
  color: var(--stitch-text-primary);
  margin-bottom: var(--stitch-spacing-sm);
}

.empty-hint {
  font-size: 26rpx;
  color: var(--stitch-text-secondary);
  margin-bottom: var(--stitch-spacing-lg);
}

.btn-refresh {
  min-width: 200rpx;
}

/* ========== 订单卡片 ========== */
.order-card {
  position: relative;
  padding: var(--stitch-spacing-md);
  margin-bottom: var(--stitch-spacing-md);
  background: rgba(255, 255, 255, 0.7);
  backdrop-filter: blur(20rpx);
  -webkit-backdrop-filter: blur(20rpx);
  border-radius: var(--stitch-radius-lg);
  transition: all var(--stitch-transition-base);
  overflow: hidden;
}

.order-card:active {
  transform: scale(0.98);
}

.order-card.urgent {
  border-left: 6rpx solid var(--stitch-warning);
}

.urgent-tag {
  position: absolute;
  top: var(--stitch-spacing-md);
  right: var(--stitch-spacing-md);
  width: 48rpx;
  height: 48rpx;
  background: linear-gradient(135deg, var(--stitch-warning) 0%, #F57C00 100%);
  color: var(--stitch-bg-white);
  border-radius: var(--stitch-radius-sm);
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 28rpx;
  font-weight: bold;
}

.order-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: var(--stitch-spacing-md);
}

.order-title-section {
  flex: 1;
}

.order-title {
  display: block;
  font-size: 32rpx;
  font-weight: 600;
  color: var(--stitch-text-primary);
  margin-bottom: 8rpx;
}

.order-type {
  display: inline-block;
  padding: 4rpx 16rpx;
  background: rgba(74, 93, 78, 0.1);
  color: var(--stitch-green-primary);
  border-radius: var(--stitch-radius-sm);
  font-size: 22rpx;
}

.order-price {
  font-size: 40rpx;
  font-weight: 700;
  color: var(--stitch-green-primary);
  margin-left: var(--stitch-spacing-md);
}

/* ========== 订单内容 ========== */
.order-body {
  margin-bottom: var(--stitch-spacing-md);
  padding-bottom: var(--stitch-spacing-md);
  border-bottom: 1rpx solid rgba(74, 93, 78, 0.1);
}

.order-info-row {
  display: flex;
  align-items: flex-start;
  margin-bottom: var(--stitch-spacing-sm);
}

.order-info-row:last-of-type {
  margin-bottom: 0;
}

.info-icon {
  font-size: 28rpx;
  margin-right: 8rpx;
  flex-shrink: 0;
}

.info-text {
  font-size: 26rpx;
  color: var(--stitch-text-secondary);
  flex: 1;
  line-height: 1.5;
}

.info-distance {
  font-size: 24rpx;
  color: var(--stitch-green-primary);
  margin-left: var(--stitch-spacing-sm);
}

/* ========== 订单底部 ========== */
.order-footer {
  display: flex;
  justify-content: space-between;
  align-items: center;
}

.order-time {
  font-size: 24rpx;
  color: var(--stitch-text-secondary);
}

.btn-grab {
  min-width: 200rpx;
  padding: 0 var(--stitch-spacing-lg);
}

.btn-grab.grabbing {
  opacity: 0.6;
}

/* ========== 加载状态 ========== */
.loading-state {
  display: flex;
  flex-direction: column;
  align-items: center;
  padding: var(--stitch-spacing-lg);
}

.loading-icon {
  font-size: 48rpx;
  margin-bottom: var(--stitch-spacing-sm);
  animation: spin 1s linear infinite;
}

.loading-text {
  font-size: 26rpx;
  color: var(--stitch-text-secondary);
}

@keyframes spin {
  from { transform: rotate(0deg); }
  to { transform: rotate(360deg); }
}

/* ========== 没有更多 ========== */
.no-more-state {
  padding: var(--stitch-spacing-lg);
  text-align: center;
}

.no-more-text {
  font-size: 26rpx;
  color: var(--stitch-text-secondary);
}
