@import "../../theme.wxss";

/* 护生证书预览页样式 */

.container {
  min-height: 100vh;
  background-color: var(--stitch-bg-primary);
  padding-bottom: 160rpx;
}

/* 自定义导航栏 */
.custom-nav {
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  background: transparent;
  z-index: 100;
  padding: 20rpx var(--stitch-spacing-md);
}

.nav-back {
  display: inline-flex;
  align-items: center;
  padding: 16rpx 24rpx;
  background: rgba(255, 255, 255, 0.9);
  backdrop-filter: blur(10rpx);
  border-radius: var(--radius-full);
  box-shadow: 0 4rpx 12rpx rgba(0, 0, 0, 0.1);
}

.back-icon {
  font-size: 40rpx;
  color: var(--stitch-text-primary);
  font-weight: 300;
  line-height: 1;
  margin-right: 8rpx;
}

.back-text {
  font-size: 26rpx;
  color: var(--stitch-text-primary);
}

/* 证书展示区 */
.cert-display {
  padding: calc(var(--stitch-spacing-md) + 80rpx) var(--stitch-spacing-md) var(--stitch-spacing-md);
  display: flex;
  justify-content: center;
}

.cert-image-wrapper {
  width: 100%;
  max-width: 600rpx;
  aspect-ratio: 9 / 16;
  border-radius: var(--stitch-radius-md);
  overflow: hidden;
  box-shadow: 0 8rpx 32rpx rgba(0, 0, 0, 0.15);
  background: var(--stitch-bg-white);
}

.cert-image {
  width: 100%;
  height: 100%;
}

/* 证书信息区 */
.cert-info {
  margin: var(--stitch-spacing-md);
  background: var(--stitch-bg-white);
  border-radius: var(--stitch-radius-md);
  padding: var(--stitch-spacing-md);
}

.info-item {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 20rpx 0;
  border-bottom: 1rpx solid rgba(74, 93, 78, 0.03);
}

.info-item:last-child {
  border-bottom: none;
}

.info-label {
  font-size: 28rpx;
  color: var(--stitch-text-secondary);
}

.info-value {
  font-size: 28rpx;
  color: var(--stitch-text-primary);
  font-weight: 500;
}

.cert-no {
  font-family: 'Courier New', monospace;
  color: var(--stitch-gold-primary);
}

.info-content {
  margin-top: var(--stitch-spacing-sm);
  padding-top: var(--stitch-spacing-sm);
  border-top: 1rpx solid rgba(74, 93, 78, 0.03);
}

.content-label {
  display: block;
  font-size: 26rpx;
  color: var(--stitch-text-secondary);
  margin-bottom: 16rpx;
}

.content-text {
  display: block;
  font-size: 28rpx;
  color: var(--stitch-text-primary);
  line-height: 1.8;
  text-indent: 2em;
}

/* 底部操作区 */
.footer {
  position: fixed;
  bottom: 0;
  left: 0;
  right: 0;
  background: var(--stitch-bg-white);
  padding: var(--stitch-spacing-md);
  border-top: 1rpx solid rgba(74, 93, 78, 0.03);
  display: flex;
  gap: var(--stitch-spacing-sm);
  box-shadow: 0 -4rpx 12rpx rgba(0, 0, 0, 0.05);
}

.btn-action {
  flex: 1;
  height: 88rpx;
  font-size: 30rpx;
  font-weight: 600;
  border-radius: var(--stitch-radius-md);
  border: none;
  display: flex;
  align-items: center;
  justify-content: center;
  transition: all var(--stitch-transition-base);
}

.btn-save {
  background: linear-gradient(135deg, var(--stitch-gold-primary) 0%, var(--stitch-gold-dim) 100%);
  color: var(--stitch-bg-white);
}

.btn-save:active {
  opacity: 0.8;
  transform: scale(0.98);
}

.btn-share {
  background: transparent;
  color: var(--stitch-gold-primary);
  border: 2rpx solid var(--stitch-gold-primary);
}

.btn-share:active {
  background: rgba(212, 184, 123, 0.1);
}
