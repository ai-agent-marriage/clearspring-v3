@import "../../theme.wxss";

/* 清如 ClearSpring - 机构结算管理页样式 */

.container {
  padding: var(--stitch-spacing-md);
  padding-bottom: var(--stitch-spacing-xxxl);
  background: linear-gradient(180deg, var(--stitch-bg-primary) 0%, var(--stitch-bg-secondary) 100%);
  min-height: 100vh;
}

/* ========== 顶部导航栏 ========== */
.header {
  display: flex;
  justify-content: center;
  align-items: center;
  padding: var(--stitch-spacing-md);
  margin-bottom: var(--stitch-spacing-md);
  background: rgba(212, 184, 123, 0.95);
  backdrop-filter: blur(20rpx);
  -webkit-backdrop-filter: blur(20rpx);
  border-radius: var(--stitch-radius-lg);
}

.header-title {
  font-size: 36rpx;
  font-weight: 700;
  color: var(--stitch-bg-white);
}

/* ========== 顶部数据卡片 ========== */
.stats-card {
  padding: var(--stitch-spacing-md);
  margin-bottom: var(--stitch-spacing-md);
  background: linear-gradient(180deg, var(--stitch-bg-white) 0%, var(--stitch-bg-tertiary) 100%);
  border-radius: var(--stitch-radius-lg);
}

.stats-row {
  display: flex;
  justify-content: space-around;
  align-items: center;
}

.stat-item {
  flex: 1;
  display: flex;
  flex-direction: column;
  align-items: center;
}

.stat-label {
  font-size: 24rpx;
  color: var(--stitch-text-secondary);
  margin-bottom: 8rpx;
}

.stat-value {
  font-size: 36rpx;
  font-weight: 700;
}

.stat-divider {
  width: 1rpx;
  height: 60rpx;
  background: rgba(74, 93, 78, 0.1);
}

.text-gold {
  color: var(--stitch-gold-dim);
}

.text-warning {
  color: var(--stitch-warning);
}

.text-green {
  color: var(--stitch-green-primary);
}

/* ========== Tab 栏 ========== */
.tabs-scroll {
  white-space: nowrap;
  margin-bottom: var(--stitch-spacing-md);
}

.tabs-container {
  display: inline-flex;
  gap: var(--stitch-spacing-sm);
  padding-right: var(--stitch-spacing-md);
}

.tab-item {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  padding: 12rpx 32rpx;
  background: rgba(255, 255, 255, 0.7);
  border-radius: var(--stitch-radius-sm);
  font-size: 26rpx;
  font-weight: 500;
  color: var(--stitch-text-secondary);
  transition: all var(--stitch-transition-fast);
}

.tab-item.active {
  background: linear-gradient(135deg, var(--stitch-gold-primary) 0%, var(--stitch-gold-dim) 100%);
  color: var(--stitch-bg-white);
}

.tab-item:active {
  opacity: 0.8;
  transform: scale(0.98);
}

/* ========== Tab 内容区 ========== */
.tab-content {
  padding-bottom: var(--stitch-spacing-md);
}

/* ========== 批量操作 ========== */
.batch-action {
  margin-bottom: var(--stitch-spacing-md);
}

.btn-batch {
  width: 100%;
  min-height: 88rpx;
  background: linear-gradient(135deg, var(--stitch-gold-primary) 0%, var(--stitch-gold-dim) 100%);
  color: var(--stitch-bg-white);
  font-size: 30rpx;
  font-weight: 600;
  border-radius: var(--stitch-radius-lg);
  border: none;
}

.btn-batch:active {
  opacity: 0.8;
  transform: scale(0.98);
}

/* ========== 待结算订单列表 ========== */
.pending-list {
  padding-bottom: var(--stitch-spacing-md);
}

.pending-card {
  padding: var(--stitch-spacing-md);
  margin-bottom: var(--stitch-spacing-md);
  background: linear-gradient(180deg, var(--stitch-bg-white) 0%, var(--stitch-bg-tertiary) 100%);
  border-radius: var(--stitch-radius-lg);
}

.pending-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: var(--stitch-spacing-md);
  padding-bottom: var(--stitch-spacing-md);
  border-bottom: 1rpx solid rgba(74, 93, 78, 0.04);
}

.pending-no {
  font-size: 28rpx;
  font-weight: 600;
  color: var(--stitch-text-primary);
}

.pending-deadline {
  display: flex;
  flex-direction: column;
  align-items: flex-end;
}

.deadline-label {
  font-size: 22rpx;
  color: var(--stitch-text-secondary);
}

.deadline-value {
  font-size: 26rpx;
  font-weight: 500;
  color: var(--stitch-warning);
}

.pending-info {
  margin-bottom: var(--stitch-spacing-md);
}

.info-row {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 12rpx 0;
}

.info-label {
  font-size: 26rpx;
  color: var(--stitch-text-secondary);
}

.info-value {
  font-size: 28rpx;
  font-weight: 500;
  color: var(--stitch-text-primary);
}

.pending-footer {
  display: flex;
  justify-content: flex-end;
  padding-top: var(--stitch-spacing-md);
  border-top: 1rpx solid rgba(74, 93, 78, 0.04);
}

.btn-detail {
  min-width: 160rpx;
  min-height: 64rpx;
  background: transparent;
  color: var(--stitch-gold-primary);
  font-size: 26rpx;
  border-radius: var(--stitch-radius-sm);
  border: 2rpx solid var(--stitch-gold-primary);
}

.btn-detail:active {
  opacity: 0.8;
  transform: scale(0.98);
}

/* ========== 结算记录列表 ========== */
.records-list {
  padding-bottom: var(--stitch-spacing-md);
}

.record-card {
  padding: var(--stitch-spacing-md);
  margin-bottom: var(--stitch-spacing-md);
  background: linear-gradient(180deg, var(--stitch-bg-white) 0%, var(--stitch-bg-tertiary) 100%);
  border-radius: var(--stitch-radius-lg);
}

.record-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: var(--stitch-spacing-md);
  padding-bottom: var(--stitch-spacing-md);
  border-bottom: 1rpx solid rgba(74, 93, 78, 0.04);
}

.record-no {
  font-size: 26rpx;
  font-weight: 600;
  color: var(--stitch-text-primary);
}

.record-status {
  padding: 6rpx 20rpx;
  background: rgba(0, 138, 23, 0.1);
  border-radius: var(--stitch-radius-sm);
  font-size: 24rpx;
  font-weight: 500;
  color: var(--stitch-success);
}

.record-footer {
  display: flex;
  gap: var(--stitch-spacing-sm);
  padding-top: var(--stitch-spacing-md);
  border-top: 1rpx solid rgba(74, 93, 78, 0.04);
}

.btn-export {
  flex: 1;
  min-height: 64rpx;
  background: rgba(74, 93, 78, 0.1);
  color: var(--stitch-green-primary);
  font-size: 24rpx;
  border-radius: var(--stitch-radius-sm);
  border: none;
}

.btn-detail-outline {
  flex: 1;
  min-height: 64rpx;
  background: transparent;
  color: var(--stitch-gold-primary);
  font-size: 24rpx;
  border-radius: var(--stitch-radius-sm);
  border: 2rpx solid var(--stitch-gold-primary);
}

/* ========== 发票管理 ========== */
.invoice-card {
  padding: var(--stitch-spacing-md);
  margin-bottom: var(--stitch-spacing-md);
  background: linear-gradient(180deg, var(--stitch-bg-white) 0%, var(--stitch-bg-tertiary) 100%);
  border-radius: var(--stitch-radius-lg);
}

.invoice-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: var(--stitch-spacing-md);
}

.invoice-title {
  font-size: 30rpx;
  font-weight: 600;
  color: var(--stitch-text-primary);
}

.invoice-status {
  padding: 6rpx 20rpx;
  border-radius: var(--stitch-radius-sm);
  font-size: 24rpx;
  font-weight: 500;
}

.invoice-status.status-success {
  background: rgba(0, 138, 23, 0.1);
  color: var(--stitch-success);
}

.invoice-status.status-warning {
  background: rgba(255, 154, 68, 0.1);
  color: var(--stitch-warning);
}

.invoice-info-preview {
  padding: var(--stitch-spacing-md);
  background: rgba(212, 184, 123, 0.05);
  border-radius: var(--stitch-radius-sm);
  margin-bottom: var(--stitch-spacing-md);
}

.preview-row {
  display: flex;
  justify-content: space-between;
  padding: 12rpx 0;
}

.preview-label {
  font-size: 26rpx;
  color: var(--stitch-text-secondary);
}

.preview-value {
  font-size: 28rpx;
  font-weight: 500;
  color: var(--stitch-text-primary);
}

.invoice-empty {
  padding: var(--stitch-spacing-lg);
  text-align: center;
  margin-bottom: var(--stitch-spacing-md);
}

.empty-desc {
  font-size: 28rpx;
  color: var(--stitch-text-secondary);
}

.btn-edit {
  width: 100%;
  min-height: 88rpx;
  background: linear-gradient(135deg, var(--stitch-gold-primary) 0%, var(--stitch-gold-dim) 100%);
  color: var(--stitch-bg-white);
  font-size: 28rpx;
  font-weight: 600;
  border-radius: var(--stitch-radius-md);
  border: none;
}

.upload-section {
  padding: var(--stitch-spacing-md);
  margin-bottom: var(--stitch-spacing-md);
  background: linear-gradient(180deg, var(--stitch-bg-white) 0%, var(--stitch-bg-tertiary) 100%);
  border-radius: var(--stitch-radius-lg);
}

.section-title {
  font-size: 30rpx;
  font-weight: 600;
  color: var(--stitch-text-primary);
  margin-bottom: var(--stitch-spacing-md);
  padding-left: var(--stitch-spacing-sm);
  border-left: 6rpx solid var(--stitch-gold-primary);
}

.upload-box {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  padding: var(--stitch-spacing-xxxl);
  background: rgba(212, 184, 123, 0.05);
  border: 2rpx dashed var(--stitch-gold-primary);
  border-radius: var(--stitch-radius-md);
}

.upload-icon {
  font-size: 64rpx;
  margin-bottom: var(--stitch-spacing-sm);
}

.upload-text {
  font-size: 28rpx;
  color: var(--stitch-gold-dim);
}

.history-section {
  padding-bottom: var(--stitch-spacing-md);
}

.section-header {
  font-size: 30rpx;
  font-weight: 600;
  color: var(--stitch-text-primary);
  margin-bottom: var(--stitch-spacing-md);
  padding: 0 var(--stitch-spacing-sm);
}

.history-card {
  padding: var(--stitch-spacing-md);
  margin-bottom: var(--stitch-spacing-md);
  background: linear-gradient(180deg, var(--stitch-bg-white) 0%, var(--stitch-bg-tertiary) 100%);
  border-radius: var(--stitch-radius-lg);
}

.history-row {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: var(--stitch-spacing-md);
}

.history-info {
  display: flex;
  flex-direction: column;
}

.history-no {
  font-size: 28rpx;
  font-weight: 600;
  color: var(--stitch-text-primary);
  margin-bottom: 8rpx;
}

.history-time {
  font-size: 24rpx;
  color: var(--stitch-text-secondary);
}

.history-amount {
  font-size: 32rpx;
  font-weight: 700;
  color: var(--stitch-gold-dim);
}

.history-footer {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding-top: var(--stitch-spacing-md);
  border-top: 1rpx solid rgba(74, 93, 78, 0.04);
}

.history-status {
  padding: 6rpx 20rpx;
  background: rgba(0, 138, 23, 0.1);
  border-radius: var(--stitch-radius-sm);
  font-size: 24rpx;
  font-weight: 500;
  color: var(--stitch-success);
}

.btn-view {
  min-width: 100rpx;
  min-height: 56rpx;
  background: transparent;
  color: var(--stitch-gold-primary);
  font-size: 24rpx;
  border-radius: var(--stitch-radius-sm);
  border: 2rpx solid var(--stitch-gold-primary);
}

/* ========== 空状态 ========== */
.empty-state {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  padding: var(--stitch-spacing-xxxl);
  background: rgba(255, 255, 255, 0.5);
  border-radius: var(--stitch-radius-lg);
  min-height: 400rpx;
}

.empty-icon {
  font-size: 100rpx;
  margin-bottom: var(--stitch-spacing-md);
}

.empty-text {
  font-size: 30rpx;
  font-weight: 600;
  color: var(--stitch-text-primary);
}

/* ========== 发票编辑弹窗 ========== */
.modal-mask {
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: rgba(0, 0, 0, 0.5);
  display: flex;
  align-items: center;
  justify-content: center;
  z-index: 1000;
}

.modal-content {
  width: 90%;
  max-width: 600rpx;
  max-height: 80vh;
  background: var(--stitch-bg-white);
  border-radius: var(--stitch-radius-lg);
  overflow: hidden;
  display: flex;
  flex-direction: column;
}

.modal-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: var(--stitch-spacing-md);
  border-bottom: 1rpx solid rgba(74, 93, 78, 0.04);
}

.modal-title {
  font-size: 32rpx;
  font-weight: 600;
  color: var(--stitch-text-primary);
}

.modal-close {
  font-size: 40rpx;
  color: var(--stitch-text-secondary);
  padding: var(--stitch-spacing-sm);
}

.modal-body {
  flex: 1;
  overflow-y: auto;
  padding: var(--stitch-spacing-md);
}

.form-item {
  margin-bottom: var(--stitch-spacing-md);
}

.form-label {
  display: block;
  font-size: 28rpx;
  font-weight: 500;
  color: var(--stitch-text-primary);
  margin-bottom: 12rpx;
}

.form-input {
  width: 100%;
  min-height: 80rpx;
  padding: 0 var(--stitch-spacing-md);
  background: rgba(74, 93, 78, 0.05);
  border-radius: var(--stitch-radius-sm);
  font-size: 28rpx;
  color: var(--stitch-text-primary);
}

.modal-footer {
  display: flex;
  gap: var(--stitch-spacing-md);
  padding: var(--stitch-spacing-md);
  border-top: 1rpx solid rgba(74, 93, 78, 0.04);
}

.btn-cancel {
  flex: 1;
  min-height: 88rpx;
  background: rgba(74, 93, 78, 0.1);
  color: var(--stitch-green-primary);
  font-size: 28rpx;
  font-weight: 600;
  border-radius: var(--stitch-radius-sm);
  border: none;
}

.btn-save {
  flex: 1;
  min-height: 88rpx;
  background: linear-gradient(135deg, var(--stitch-gold-primary) 0%, var(--stitch-gold-dim) 100%);
  color: var(--stitch-bg-white);
  font-size: 28rpx;
  font-weight: 600;
  border-radius: var(--stitch-radius-sm);
  border: none;
}

.btn-cancel:active,
.btn-save:active {
  opacity: 0.8;
  transform: scale(0.98);
}
