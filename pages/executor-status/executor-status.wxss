@import "../../theme.wxss";

/* 清如 ClearSpring - 执行者审核状态页样式 */

.container {
  padding: var(--stitch-spacing-md);
  padding-bottom: var(--stitch-spacing-xxxl);
  background: linear-gradient(180deg, var(--stitch-bg-primary) 0%, var(--stitch-bg-secondary) 100%);
  min-height: 100vh;
}

/* ========== 状态卡片 ========== */
.status-card {
  display: flex;
  align-items: center;
  padding: var(--stitch-spacing-lg);
  margin-bottom: var(--stitch-spacing-md);
  background: rgba(255, 255, 255, 0.7);
  backdrop-filter: blur(20rpx);
  -webkit-backdrop-filter: blur(20rpx);
  border-radius: var(--stitch-radius-lg);
}

.status-icon {
  width: 120rpx;
  height: 120rpx;
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 64rpx;
  margin-right: var(--stitch-spacing-md);
  flex-shrink: 0;
}

.status-icon.pending {
  background: linear-gradient(135deg, #FFA726 0%, #FB8C00 100%);
}

.status-icon.approved {
  background: linear-gradient(135deg, var(--stitch-green-primary) 0%, var(--stitch-success) 100%);
}

.status-icon.rejected,
.status-icon.need 补充 {
  background: linear-gradient(135deg, var(--stitch-error) 0%, #D32F2F 100%);
}

.status-info {
  flex: 1;
}

.status-title {
  display: block;
  font-size: 36rpx;
  font-weight: 600;
  color: var(--stitch-text-primary);
  margin-bottom: var(--stitch-spacing-sm);
}

.status-desc {
  display: block;
  font-size: 26rpx;
  color: var(--stitch-text-secondary);
}

/* ========== 进度条 ========== */
.progress-section {
  margin-bottom: var(--stitch-spacing-md);
  padding: var(--stitch-spacing-md);
  background: rgba(255, 255, 255, 0.7);
  backdrop-filter: blur(20rpx);
  -webkit-backdrop-filter: blur(20rpx);
  border-radius: var(--stitch-radius-lg);
}

.progress-bar {
  padding: var(--stitch-spacing-md) 0;
}

.progress-track {
  height: 8rpx;
  background: rgba(74, 93, 78, 0.1);
  border-radius: 24rpx;
  overflow: hidden;
  margin-bottom: var(--stitch-spacing-lg);
}

.progress-fill {
  height: 100%;
  background: linear-gradient(90deg, var(--stitch-green-primary) 0%, var(--stitch-success) 100%);
  border-radius: 24rpx;
  transition: width var(--transition-slow);
}

.progress-fill.completed {
  background: linear-gradient(90deg, var(--stitch-success) 0%, var(--stitch-green-primary) 100%);
}

.progress-steps {
  display: flex;
  justify-content: space-between;
}

.progress-step {
  display: flex;
  flex-direction: column;
  align-items: center;
  flex: 1;
  position: relative;
}

.step-dot {
  width: 24rpx;
  height: 24rpx;
  border-radius: 50%;
  background: rgba(74, 93, 78, 0.2);
  margin-bottom: var(--stitch-spacing-sm);
  transition: all var(--stitch-transition-base);
}

.progress-step.active .step-dot {
  background: var(--stitch-green-primary);
  transform: scale(1.2);
}

.step-text {
  font-size: 24rpx;
  color: var(--stitch-text-secondary);
  transition: color var(--stitch-transition-base);
}

.progress-step.active .step-text {
  color: var(--stitch-green-primary);
  font-weight: 500;
}

/* ========== 审核详情 ========== */
.detail-section {
  padding: var(--stitch-spacing-md);
  margin-bottom: var(--stitch-spacing-md);
  background: rgba(255, 255, 255, 0.7);
  backdrop-filter: blur(20rpx);
  -webkit-backdrop-filter: blur(20rpx);
  border-radius: var(--stitch-radius-lg);
  border-left: 6rpx solid var(--stitch-error);
}

.detail-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: var(--stitch-spacing-md);
  padding-bottom: var(--stitch-spacing-sm);
  border-bottom: 1rpx solid rgba(74, 93, 78, 0.04);
}

.detail-title {
  font-size: 30rpx;
  font-weight: 600;
  color: var(--stitch-text-primary);
}

.detail-time {
  font-size: 24rpx;
  color: var(--stitch-text-secondary);
}

.detail-content {
  padding: var(--stitch-spacing-md);
  background: var(--stitch-bg-primary);
  border-radius: var(--stitch-radius-md);
  margin-bottom: var(--stitch-spacing-md);
}

.detail-text {
  font-size: 28rpx;
  color: var(--stitch-text-primary);
  line-height: 1.6;
}

.detail-actions {
  display: flex;
  gap: var(--stitch-spacing-md);
}

.detail-actions .btn {
  flex: 1;
}

/* ========== 审核通过 ========== */
.success-section {
  padding: var(--stitch-spacing-lg);
  margin-bottom: var(--stitch-spacing-md);
  background: rgba(255, 255, 255, 0.7);
  backdrop-filter: blur(20rpx);
  -webkit-backdrop-filter: blur(20rpx);
  border-radius: var(--stitch-radius-lg);
  text-align: center;
}

.success-icon {
  font-size: 100rpx;
  margin-bottom: var(--stitch-spacing-md);
}

.success-title {
  display: block;
  font-size: 36rpx;
  font-weight: 600;
  color: var(--stitch-green-primary);
  margin-bottom: var(--stitch-spacing-sm);
}

.success-desc {
  display: block;
  font-size: 26rpx;
  color: var(--stitch-text-secondary);
  margin-bottom: var(--stitch-spacing-lg);
}

.btn-start {
  width: 100%;
}

/* ========== 审核中 ========== */
.pending-section {
  margin-bottom: var(--stitch-spacing-md);
}

.pending-tip {
  display: flex;
  align-items: center;
  padding: var(--stitch-spacing-md);
  background: rgba(74, 93, 78, 0.1);
  border-radius: var(--stitch-radius-md);
  margin-bottom: var(--stitch-spacing-md);
}

.tip-icon {
  font-size: 36rpx;
  margin-right: var(--stitch-spacing-sm);
}

.tip-text {
  font-size: 26rpx;
  color: var(--stitch-text-secondary);
  flex: 1;
}

.pending-actions {
  display: flex;
  gap: var(--stitch-spacing-md);
}

.pending-actions .btn {
  flex: 1;
}

.btn-outline {
  min-height: 88rpx;
  background: transparent;
  border: 2rpx solid var(--stitch-green-primary);
  color: var(--stitch-green-primary);
  border-radius: var(--stitch-radius-md);
  font-size: 28rpx;
}

.btn-outline:active {
  background: rgba(74, 93, 78, 0.1);
}

/* ========== 时间线 ========== */
.timeline-section {
  padding: var(--stitch-spacing-md);
  background: rgba(255, 255, 255, 0.7);
  backdrop-filter: blur(20rpx);
  -webkit-backdrop-filter: blur(20rpx);
  border-radius: var(--stitch-radius-lg);
}

.timeline-title {
  font-size: 30rpx;
  font-weight: 600;
  color: var(--stitch-text-primary);
  margin-bottom: var(--stitch-spacing-md);
  padding-left: var(--stitch-spacing-sm);
  border-left: 6rpx solid var(--stitch-green-primary);
}

.timeline-list {
  padding-left: var(--stitch-spacing-sm);
}

.timeline-item {
  display: flex;
  padding: var(--stitch-spacing-md) 0;
  position: relative;
}

.timeline-item:last-of-type {
  padding-bottom: 0;
}

.timeline-dot {
  width: 20rpx;
  height: 20rpx;
  border-radius: 50%;
  background: var(--stitch-green-primary);
  margin-right: var(--stitch-spacing-md);
  flex-shrink: 0;
  position: relative;
}

.timeline-dot::after {
  content: '';
  position: absolute;
  top: 20rpx;
  left: 50%;
  transform: translateX(-50%);
  width: 2rpx;
  height: calc(100% + var(--stitch-spacing-md));
  background: rgba(74, 93, 78, 0.2);
}

.timeline-item:last-of-type .timeline-dot::after {
  display: none;
}

.timeline-content {
  flex: 1;
}

.timeline-event {
  display: block;
  font-size: 28rpx;
  color: var(--stitch-text-primary);
  margin-bottom: 4rpx;
}

.timeline-time {
  display: block;
  font-size: 24rpx;
  color: var(--stitch-text-secondary);
}
