@import "../../theme.wxss";

/* pages/wiki/detail.wxss */

page {
  background-color: var(--stitch-bg-secondary);
  --spacing-1: 16rpx;
  --spacing-2: 24rpx;
  --spacing-3: 32rpx;
  --spacing-4: 48rpx;
  --radius-sm: 8rpx;
  --radius-md: 16rpx;
  --radius-lg: 24rpx;
  --text-main: var(--stitch-text-primary);
  --text-dim: var(--stitch-text-tertiary);
  --gold-main: #D4B87B;
  --gold-dim: #6E5E00;
  --success: var(--stitch-green-primary);
  --error-soft: #FF6B6B;
}

.container {
  padding-bottom: calc(140rpx + env(safe-area-inset-bottom));
}

/* ========== 顶部导航 ========== */

.detail-header {
  position: sticky;
  top: 0;
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: var(--stitch-spacing-md);
  background: rgba(255, 255, 255, 0.95);
  backdrop-filter: blur(20rpx);
  z-index: 100;
  border-bottom: 1rpx solid #f0f0f0;
}

.header-left {
  display: flex;
  align-items: center;
  gap: 8rpx;
}

.back-icon {
  font-size: 48rpx;
  color: var(--stitch-text-primary);
  line-height: 1;
}

.back-text {
  font-size: 28rpx;
  color: var(--stitch-text-primary);
}

.header-right {
  display: flex;
  align-items: center;
  gap: var(--stitch-spacing-md);
}

.icon-btn {
  padding: 16rpx;
}

.favorite-icon {
  font-size: 40rpx;
}

.share-btn {
  padding: 16rpx;
  background: none;
  border: none;
  font-size: 40rpx;
  color: var(--stitch-text-primary);
  line-height: 1;
}

.share-btn::after {
  border: none;
}

/* ========== 详情内容 ========== */

.detail-content {
  padding: var(--stitch-spacing-md);
}

/* ========== 物种图片 ========== */

.species-image-wrapper {
  position: relative;
  width: 100%;
  height: 400rpx;
  border-radius: var(--stitch-radius-lg);
  overflow: hidden;
  margin-bottom: var(--stitch-spacing-md);
}

.species-image {
  width: 100%;
  height: 100%;
  background: linear-gradient(135deg, var(--stitch-bg-secondary) 0%, #e0e0e0 100%);
}

.species-category {
  position: absolute;
  top: var(--stitch-spacing-md);
  right: var(--stitch-spacing-md);
  padding: 8rpx 24rpx;
  background: rgba(7, 193, 96, 0.9);
  color: var(--stitch-bg-white);
  font-size: 24rpx;
  border-radius: 32rpx;
}

/* ========== 基本信息 ========== */

.info-section {
  background: var(--stitch-bg-white);
  border-radius: var(--stitch-radius-lg);
  padding: var(--stitch-spacing-md);
  margin-bottom: var(--stitch-spacing-md);
}

.info-header {
  margin-bottom: var(--stitch-spacing-md);
}

.species-name {
  display: block;
  font-size: 40rpx;
  font-weight: 700;
  font-family: 'Songti SC', 'STSong', 'SimSun', serif;
  color: var(--stitch-text-primary);
  margin-bottom: 8rpx;
}

.species-latin {
  display: block;
  font-size: 24rpx;
  font-style: italic;
  color: var(--stitch-text-secondary);
  font-family: 'Times New Roman', serif;
}

.info-grid {
  display: grid;
  grid-template-columns: repeat(2, 1fr);
  gap: var(--stitch-spacing-md);
}

.info-item {
  display: flex;
  flex-direction: column;
  gap: 8rpx;
}

.info-item.full-width {
  grid-column: 1 / -1;
}

.info-label {
  font-size: 24rpx;
  color: var(--stitch-text-secondary);
}

.info-value {
  font-size: 28rpx;
  color: var(--stitch-text-primary);
  font-weight: 500;
}

/* ========== 描述区域 ========== */

.desc-section,
.tips-section,
.warnings-section,
.article-header,
.article-content,
.guide-header,
.guide-content,
.references-section {
  background: var(--stitch-bg-white);
  border-radius: var(--stitch-radius-lg);
  padding: var(--stitch-spacing-md);
  margin-bottom: var(--stitch-spacing-md);
}

.section-title {
  font-size: 32rpx;
  font-weight: 600;
  font-family: 'Songti SC', 'STSong', 'SimSun', serif;
  color: var(--stitch-text-primary);
  margin-bottom: var(--stitch-spacing-md);
  padding-bottom: var(--stitch-spacing-md);
  border-bottom: 1rpx solid #f0f0f0;
}

.section-title.warning {
  color: var(--stitch-error);
}

.desc-text {
  font-size: 28rpx;
  color: var(--stitch-text-primary);
  line-height: 1.8;
  display: block;
}

.feature-tags {
  display: flex;
  flex-wrap: wrap;
  gap: 12rpx;
  margin-top: var(--stitch-spacing-md);
}

.feature-tag {
  padding: 8rpx 20rpx;
  background: rgba(212, 184, 123, 0.15);
  color: var(--stitch-gold-dim);
  font-size: 24rpx;
  border-radius: 32rpx;
}

/* ========== 放生建议 ========== */

.tip-item {
  display: flex;
  align-items: flex-start;
  gap: var(--stitch-spacing-md);
  padding: var(--stitch-spacing-sm) 0;
}

.tip-icon {
  font-size: 28rpx;
  color: var(--stitch-success);
  flex-shrink: 0;
}

.tip-text {
  font-size: 26rpx;
  color: var(--stitch-text-primary);
  line-height: 1.6;
  flex: 1;
}

/* ========== 注意事项 ========== */

.warning-item {
  display: flex;
  align-items: flex-start;
  gap: var(--stitch-spacing-md);
  padding: var(--stitch-spacing-sm) 0;
}

.warning-icon {
  width: 36rpx;
  height: 36rpx;
  line-height: 36rpx;
  text-align: center;
  background: linear-gradient(135deg, #FF9A44 0%, #FF6B35 100%);
  color: var(--stitch-bg-white);
  font-size: 24rpx;
  border-radius: 50%;
  flex-shrink: 0;
}

.warning-text {
  font-size: 26rpx;
  color: var(--stitch-text-primary);
  line-height: 1.6;
  flex: 1;
}

/* ========== 文章样式 ========== */

.article-title {
  display: block;
  font-size: 36rpx;
  font-weight: 700;
  font-family: 'Songti SC', 'STSong', 'SimSun', serif;
  color: var(--stitch-text-primary);
  margin-bottom: var(--stitch-spacing-md);
  line-height: 1.4;
}

.article-meta {
  display: flex;
  gap: var(--stitch-spacing-md);
  margin-bottom: var(--stitch-spacing-md);
  flex-wrap: wrap;
}

.meta-item {
  font-size: 24rpx;
  color: var(--stitch-text-secondary);
}

.article-tags {
  display: flex;
  flex-wrap: wrap;
  gap: 12rpx;
}

.article-tags .tag {
  padding: 8rpx 20rpx;
  background: rgba(212, 184, 123, 0.15);
  color: var(--stitch-gold-dim);
  font-size: 24rpx;
  border-radius: 32rpx;
}

.markdown-content {
  font-size: 28rpx;
  color: var(--stitch-text-primary);
  line-height: 2;
  white-space: pre-wrap;
}

.content-text {
  display: block;
}

/* ========== 指南样式 ========== */

.guide-category {
  display: inline-block;
  padding: 8rpx 24rpx;
  background: linear-gradient(135deg, var(--stitch-gold-primary) 0%, var(--stitch-gold-dim) 100%);
  color: var(--stitch-bg-white);
  font-size: 24rpx;
  border-radius: 32rpx;
  margin-bottom: var(--stitch-spacing-md);
}

.guide-title {
  display: block;
  font-size: 36rpx;
  font-weight: 700;
  font-family: 'Songti SC', 'STSong', 'SimSun', serif;
  color: var(--stitch-text-primary);
  margin-bottom: var(--stitch-spacing-sm);
  line-height: 1.4;
}

.guide-update {
  display: block;
  font-size: 24rpx;
  color: var(--stitch-text-secondary);
  margin-top: var(--stitch-spacing-sm);
}

/* ========== 参考依据 ========== */

.reference-item {
  padding: var(--stitch-spacing-sm) 0;
  border-left: 4rpx solid var(--stitch-gold-primary);
  padding-left: var(--stitch-spacing-md);
}

.reference-text {
  font-size: 26rpx;
  color: var(--stitch-text-primary);
  line-height: 1.6;
}

/* ========== 底部操作栏 ========== */

.bottom-bar {
  position: fixed;
  bottom: 0;
  left: 0;
  right: 0;
  display: flex;
  align-items: center;
  padding: calc(var(--stitch-spacing-md) + env(safe-area-inset-bottom)) var(--stitch-spacing-md) var(--stitch-spacing-md);
  background: var(--stitch-bg-white);
  border-top: 1rpx solid #f0f0f0;
  box-shadow: 0 -4rpx 20rpx rgba(0, 0, 0, 0.05);
  z-index: 100;
}

.bar-item {
  flex: 1;
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 4rpx;
  padding: var(--stitch-spacing-sm);
  background: none;
  border: none;
}

.bar-item::after {
  border: none;
}

.bar-icon {
  font-size: 40rpx;
}

.bar-text {
  font-size: 22rpx;
  color: var(--stitch-text-secondary);
}

.share-bar-item {
  background: none;
  border: none;
}

.share-bar-item::after {
  border: none;
}
