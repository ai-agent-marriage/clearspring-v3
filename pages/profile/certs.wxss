@import "../../theme.wxss";

/* pages/profile/certs.wxss - 证书管理页面样式优化 */

page {
  background-color: var(--stitch-bg-secondary);
  padding-bottom: 40rpx;
}

.container {
  min-height: 100vh;
}

/* ========== 顶部操作栏 ========== */
.header-bar {
  display: flex;
  justify-content: space-between;
  align-items: center;
  background-color: var(--stitch-bg-white);
  padding: 24rpx 32rpx;
  margin-bottom: 20rpx;
  box-shadow: 0 2rpx 8rpx rgba(0, 0, 0, 0.05);
}

.header-title {
  font-size: 32rpx;
  font-weight: 600;
  color: var(--stitch-text-primary);
}

.header-actions .action-btn {
  font-size: 26rpx;
  color: var(--stitch-green-primary);
  padding: 12rpx 24rpx;
  background-color: var(--stitch-bg-primary);
  border-radius: 24rpx;
  font-weight: 500;
}

/* ========== 分类筛选 ========== */
.category-tabs {
  background-color: var(--stitch-bg-white);
  padding: 20rpx 0;
  margin-bottom: 20rpx;
  box-shadow: 0 2rpx 8rpx rgba(0, 0, 0, 0.05);
}

.tabs-scroll {
  white-space: nowrap;
}

.tab-item {
  display: inline-block;
  padding: 16rpx 32rpx;
  font-size: 28rpx;
  color: var(--stitch-text-secondary);
  position: relative;
  transition: all 0.3s ease;
}

.tab-item.active {
  color: var(--stitch-green-primary);
  font-weight: 600;
}

.tab-indicator {
  position: absolute;
  bottom: 8rpx;
  left: 50%;
  transform: translateX(-50%);
  width: 40rpx;
  height: 4rpx;
  background-color: var(--stitch-green-primary);
  border-radius: 24rpx;
}

/* ========== 排序栏 ========== */
.sort-bar {
  display: flex;
  align-items: center;
  background-color: var(--stitch-bg-white);
  padding: 20rpx 32rpx;
  margin-bottom: 20rpx;
  gap: 24rpx;
}

.sort-label {
  font-size: 26rpx;
  color: var(--stitch-text-tertiary);
}

.sort-item {
  font-size: 26rpx;
  color: var(--stitch-text-secondary);
  padding: 8rpx 16rpx;
  border-radius: 24rpx;
  transition: all 0.2s ease;
}

.sort-item.active {
  color: var(--stitch-green-primary);
  background-color: var(--stitch-bg-primary);
  font-weight: 500;
}

/* ========== 批量操作栏 ========== */
.batch-bar {
  display: flex;
  justify-content: space-between;
  align-items: center;
  background-color: var(--stitch-green-primary);
  padding: 20rpx 32rpx;
  margin: 0 24rpx 20rpx;
  border-radius: 24rpx;
  box-shadow: 0 4rpx 12rpx rgba(7, 193, 96, 0.3);
}

.batch-info {
  font-size: 28rpx;
  color: var(--stitch-bg-white);
  font-weight: 500;
}

.batch-actions {
  display: flex;
  gap: 16rpx;
}

.batch-btn {
  padding: 12rpx 28rpx;
  border-radius: 24rpx;
  font-size: 26rpx;
  color: var(--stitch-green-primary);
  background-color: var(--stitch-bg-white);
  font-weight: 500;
  transition: all 0.2s ease;
}

.batch-btn.primary {
  color: var(--stitch-bg-white);
  background-color: rgba(255, 255, 255, 0.2);
  border: 1rpx solid rgba(255, 255, 255, 0.08);
}

/* ========== 瀑布流布局 ========== */
.waterfall-container {
  display: flex;
  padding: 0 16rpx;
  gap: 16rpx;
}

.waterfall-column {
  flex: 1;
  display: flex;
  flex-direction: column;
  gap: 16rpx;
}

.cert-card {
  background-color: var(--stitch-bg-white);
  border-radius: 24rpx;
  overflow: hidden;
  box-shadow: 0 4rpx 12rpx rgba(0, 0, 0, 0.06);
  position: relative;
  transition: all 0.2s ease;
}

.cert-card:active {
  transform: scale(0.98);
}

/* 选择框（批量模式） */
.select-checkbox {
  position: absolute;
  top: 20rpx;
  left: 20rpx;
  width: 40rpx;
  height: 40rpx;
  background-color: rgba(255, 255, 255, 0.9);
  border: 2rpx solid #ddd;
  border-radius: 50%;
  z-index: 10;
  display: flex;
  align-items: center;
  justify-content: center;
}

.select-checkbox.checked {
  background-color: var(--stitch-green-primary);
  border-color: var(--stitch-green-primary);
}

.select-checkbox image {
  width: 24rpx;
  height: 24rpx;
}

/* 证书图片 */
.cert-image {
  width: 100%;
  aspect-ratio: 3/4;
  background-color: var(--stitch-bg-primary);
}

/* 证书类型标签 */
.cert-type-tag {
  position: absolute;
  top: 20rpx;
  right: 20rpx;
  padding: 8rpx 20rpx;
  border-radius: 24rpx;
  font-size: 22rpx;
  font-weight: 600;
  color: var(--stitch-bg-white);
  box-shadow: 0 2rpx 8rpx rgba(0, 0, 0, 0.2);
}

.cert-type-tag.protect {
  background: linear-gradient(135deg, var(--stitch-green-primary), #05a850);
}

.cert-type-tag.practice {
  background: linear-gradient(135deg, var(--stitch-gold-light), #b89f66);
}

/* 证书信息 */
.cert-info {
  padding: 20rpx;
}

.cert-title {
  font-size: 28rpx;
  font-weight: 600;
  color: var(--stitch-text-primary);
  margin-bottom: 12rpx;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.cert-meta {
  display: flex;
  justify-content: space-between;
  align-items: center;
}

.cert-date {
  font-size: 24rpx;
  color: var(--stitch-text-tertiary);
}

.cert-merit {
  font-size: 24rpx;
  color: var(--stitch-gold-light);
  font-weight: 600;
}

/* ========== 空状态 ========== */
.empty-state {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  padding: 120rpx 0;
}

.empty-icon {
  width: 200rpx;
  height: 200rpx;
  margin-bottom: 32rpx;
  opacity: 0.6;
}

.empty-text {
  font-size: 32rpx;
  color: var(--stitch-text-tertiary);
  margin-bottom: 16rpx;
  font-weight: 500;
}

.empty-hint {
  font-size: 26rpx;
  color: #bbb;
}

/* ========== 证书详情弹窗 ========== */
.detail-modal {
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  z-index: 1000;
  opacity: 0;
  visibility: hidden;
  transition: all 0.3s ease;
}

.detail-modal.show {
  opacity: 1;
  visibility: visible;
}

.modal-mask {
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background-color: rgba(0, 0, 0, 0.5);
}

.modal-content {
  position: absolute;
  bottom: 0;
  left: 0;
  right: 0;
  background-color: var(--stitch-bg-white);
  border-radius: 24rpx 24rpx 0 0;
  max-height: 85vh;
  display: flex;
  flex-direction: column;
  transform: translateY(100%);
  transition: transform 0.3s ease;
}

.detail-modal.show .modal-content {
  transform: translateY(0);
}

.modal-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 32rpx;
  border-bottom: 1rpx solid rgba(74, 93, 78, 0.03);
}

.modal-title {
  font-size: 32rpx;
  font-weight: 600;
  color: var(--stitch-text-primary);
}

.close-btn {
  width: 40rpx;
  height: 40rpx;
}

.modal-body {
  flex: 1;
  overflow-y: auto;
  padding: 32rpx;
}

.detail-section {
  display: flex;
  flex-direction: column;
  gap: 32rpx;
}

/* 证书高清图 */
.cert-image-container {
  position: relative;
  border-radius: 24rpx;
  overflow: hidden;
  box-shadow: 0 4rpx 16rpx rgba(0, 0, 0, 0.1);
}

.cert-full-image {
  width: 100%;
  display: block;
}

.image-hint {
  position: absolute;
  bottom: 0;
  left: 0;
  right: 0;
  background-color: rgba(0, 0, 0, 0.6);
  color: var(--stitch-bg-white);
  font-size: 24rpx;
  text-align: center;
  padding: 12rpx 0;
}

/* 证书详情信息 */
.cert-detail-info {
  display: flex;
  flex-direction: column;
  gap: 16rpx;
  padding: 24rpx;
  background-color: var(--stitch-bg-primary);
  border-radius: 24rpx;
}

.info-row {
  display: flex;
  justify-content: space-between;
  align-items: center;
}

.info-label {
  font-size: 26rpx;
  color: var(--stitch-text-tertiary);
}

.info-value {
  font-size: 28rpx;
  color: var(--stitch-text-primary);
  font-weight: 500;
}

.info-value.merit {
  color: var(--stitch-gold-light);
  font-weight: 700;
}

.info-value.small {
  font-size: 24rpx;
  color: var(--stitch-text-secondary);
}

/* 二维码区域 */
.qrcode-section {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 16rpx;
  padding: 24rpx;
  background-color: var(--stitch-bg-primary);
  border-radius: 24rpx;
}

.section-title {
  font-size: 28rpx;
  font-weight: 600;
  color: var(--stitch-text-primary);
}

.qrcode-image {
  width: 400rpx;
  height: 400rpx;
  border: 4rpx solid var(--stitch-bg-white);
  box-shadow: 0 4rpx 12rpx rgba(0, 0, 0, 0.1);
  border-radius: 24rpx;
}

.qrcode-hint {
  font-size: 24rpx;
  color: var(--stitch-text-tertiary);
}

/* 弹窗底部按钮 */
.modal-footer {
  display: flex;
  gap: 20rpx;
  padding: 24rpx 32rpx 40rpx;
  border-top: 1rpx solid rgba(74, 93, 78, 0.03);
}

.footer-btn {
  flex: 1;
  text-align: center;
  padding: 20rpx 0;
  border-radius: 24rpx;
  font-size: 28rpx;
  font-weight: 600;
  transition: all 0.2s ease;
}

.footer-btn.secondary {
  background-color: var(--stitch-bg-secondary);
  color: var(--stitch-text-primary);
  border: 1rpx solid rgba(74, 93, 78, 0.03);
}

.footer-btn.primary {
  background-color: var(--stitch-green-primary);
  color: var(--stitch-bg-white);
}

.footer-btn:active {
  opacity: 0.8;
  transform: scale(0.98);
}
